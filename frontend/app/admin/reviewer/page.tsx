import { ReviewerDashboard } from "@/components/reviewer-dashboard";

export const metadata = { title: "My Assigned Applications — Pravaron Careers" };

export default function ReviewerPage() {
  return <ReviewerDashboard />;
}
