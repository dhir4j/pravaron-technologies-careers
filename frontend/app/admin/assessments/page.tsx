import { AdminAssessments } from "@/components/admin-assessments";

export const metadata = { title: "Assessments — Admin — Pravaron Careers" };

export default function AssessmentsPage() {
  return <AdminAssessments />;
}
