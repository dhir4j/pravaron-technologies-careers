import { CandidateAssessments } from "@/components/candidate-assessments";

export const metadata = { title: "Assessments — Pravaron Careers" };

export default function AssessmentsPage() {
  return <CandidateAssessments />;
}
